spring:
  application:
    name: spring-admin
  cloud:
    kubernetes:
      discovery:
        enabled: true
        service-labels:
          scrape: true
  boot:
    admin:
      ui:
        title: Spring Boot Admin
        brand: Danafix Monitoring Spring Boot
  security:
    user:
      name: cak
      password: hanif

server:
  port: 8080
management:
  endpoints:
    web:
      exposure:
        include: "*"
  server:
    port: 8081